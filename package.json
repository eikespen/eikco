{
	"name": "eikco",
	"version": "0.1.0",
	"private": true,
	"description": "WordPress block theme and local dev environment for eik.es — Eik & Co buyer's agent.",
	"scripts": {
		"start": "wp-env start",
		"stop": "wp-env stop",
		"destroy": "wp-env destroy",
		"logs": "wp-env logs",
		"clean": "wp-env clean all",
		"cli": "wp-env run cli wp"
	},
	"devDependencies": {
		"@wordpress/env": "^10.0.0"
	},
	"engines": {
		"node": ">=18"
	}
}
